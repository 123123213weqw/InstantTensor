[package]
name = "stbench"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "safetensors load benchmark harness (cold cache, O_DIRECT io_uring vs buffered)"

[[bin]]
name = "stbench"
path = "src/main.rs"

[dependencies]
stloader = { path = "../stloader" }
libc = "0.2"
